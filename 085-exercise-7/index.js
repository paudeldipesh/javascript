/*
Create a JavaScript program capable of generating a password that contains at least one lowercase, one uppercase, and one special character.

Create a password class to achieve the same.
Password class:

this.password: The password property stores the password that is generated.
generateWeakPassword(): This method generates a weak password that contains only lowercase letters.
generateStrongPassword(): This method generates a strong password that contains lowercase, uppercase, and special characters.
generateSuperStrongPassword(): This method generates a super strong password that contains lowercase, uppercase, special characters, and numbers.
generateEasyGuessPassword(): This method generates an easy to guess password that contains only numbers and common words.
*/
